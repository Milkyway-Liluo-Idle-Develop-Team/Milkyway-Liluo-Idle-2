# 游戏运行和推进模式

## 推进机制

游戏采用**请求驱动推进**模式。服务器不会主动持续计算每个玩家的游戏状态，而是在收到客户端的 WebSocket 同步请求后，将对应玩家的游戏进度推进到当前时间。

### 具体流程

1. 客户端发送同步请求（如 `sync` 消息）。
2. 收到 `sync` 消息后游戏对各种系统进行结算
5. 将结算结果返回给客户端，客户端刷新 UI。

### 离线处理

玩家离线期间，事件队列不会停止。下次登录或同步时，服务端会计算离线期间应得的全部产出，一次性应用到玩家数据上。

